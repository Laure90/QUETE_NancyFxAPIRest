using System;
using System.Collections.Generic;
using System.Text;
using Nancy;

namespace QUETE_APIRestNANCY
{
    public class UserModule : NancyModule
    {
        /*public UserModule()
        {
            // Get JSON about a given user
            Get["/users/{UserId}"] = parameters => ReturnUserData(parameters.UserId);
            // Delete an existing user            
            Delete["/users/{UserId}"] = parameters => DeleteUser(parameters.UserId);
            // Add a new user
            Put["/users"] = parameters => PutNewUser();
        }*/
    }
}
